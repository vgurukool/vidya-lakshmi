apiVersion: v1
kind: Service
metadata:
  name: {{ include "vidya-lakshmi.fullname" . }}
  labels:
    {{- include "vidya-lakshmi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vidya-lakshmi.selectorLabels" . | nindent 4 }}
